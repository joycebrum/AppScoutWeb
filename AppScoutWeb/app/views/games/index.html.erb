<p id="notice"><%= notice %></p>

<h1>Games</h1>

<table>
  <thead>
    <tr> 
      <th><%= @games.human_attribute_name :title%></th>
      <th><%= @games.human_attribute_name :description%></th>
      <th><%= @games.human_attribute_name :duration_min%></th>
      <th><%= @games.human_attribute_name :duration_max%></th>
      <th><%= @games.human_attribute_name :material_list%></th>
      <th><%= @games.human_attribute_name :quant_min%></th>
      <th><%= @games.human_attribute_name :quant_max%></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.title %></td>
        <td><%= game.description %></td>
        <td><%= game.duration_min %></td>
        <td><%= game.duration_max %></td>
        <td><%= game.material_list %></td>
        <td><%= game.quant_min %></td>
        <td><%= game.quant_max %></td>
        <td><%= link_to t('form_options.show'), game %></td>
        <td><%= link_to t('form_options.edit'), edit_game_path(game) %></td>
        <td><%= link_to t('form_options.destroy'), game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Game', new_game_path %>
