<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-12 col-lg-2">
    <%= label_tag nil, @competence.class.human_attribute_name(:number) %>
    <%= text_field_tag nil, @competence.number, class: "form-control", disabled: true %>
  </div>
  <div class="col-12 col-lg-10">
    <%= label_tag nil, @competence.class.human_attribute_name(:title) %>
    <%= text_area_tag nil, @competence.title, class: "form-control", disabled: true %>
  </div>
</div>
<p>

<%= link_to t('form_options.edit'), edit_competence_path(@competence), class: "btn btn-outline-secondary mt-2" %>
<%= link_to t('form_options.back'), competences_path, class: "btn btn-outline-secondary mt-2" %>
